return {
    tooltip = require(... .. ".tooltip"),
    battery = require(... .. ".battery"),
    net_speed = require(... .. ".net_speed"),
    cpu = require(... .. ".cpu"),
    mem = require(... .. ".mem"),
    volume = require(... .. ".volume"),
    mpris = require(... .. ".mpris"),
    --exit_menu = require(... .. ".exit_menu"),
}
